#if canImport(SwiftUI)
import SwiftUI

extension Color {
    static let breezeBackground = Color(.systemBackground)
    static let breezeCard = Color(.secondarySystemBackground)
    static let breezeAccent = Color.accentColor
}
#endif
