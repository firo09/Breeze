#if canImport(SwiftUI)
import SwiftUI

struct SearchPage: View {
    var body: some View {
        NavigationStack {
            Text("Search Feature Shell")
                .navigationTitle("Search")
        }
    }
}
#endif
