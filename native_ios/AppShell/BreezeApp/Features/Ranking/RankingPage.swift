#if canImport(SwiftUI)
import SwiftUI

struct RankingPage: View {
    var body: some View {
        NavigationStack {
            Text("Ranking Feature Shell")
                .navigationTitle("Ranking")
        }
    }
}
#endif
