#if canImport(SwiftUI)
import SwiftUI

struct SettingsPage: View {
    var body: some View {
        NavigationStack {
            Text("Settings Feature Shell")
                .navigationTitle("Settings")
        }
    }
}
#endif
