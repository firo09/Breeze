#if canImport(SwiftUI)
import SwiftUI

struct BookshelfPage: View {
    var body: some View {
        NavigationStack {
            Text("Bookshelf Feature Shell")
                .navigationTitle("Bookshelf")
        }
    }
}
#endif
